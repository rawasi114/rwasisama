# وثيقة التسليم — مسار الموارد البشرية (HR)

> **الغرض:** تتبّع مراحل مسار الموارد البشرية في نظام رواسي سما، وحالة كل مرحلة،
> وما تبقّى. تُحدَّث هذه الوثيقة عند نهاية كل مرحلة (بوابة اعتماد).
>
> **آخر تحديث:** 2026-06-24 — إنجاز **HR-5**.

---

## ملاحظة عن السياق

البيئة التنفيذية مؤقّتة (ephemeral)؛ أي وثيقة تسليم سابقة لم تُحفَظ في المستودع
فُقدت مع إعادة تهيئة الحاوية. هذه الوثيقة تُعيد تأسيس التتبّع داخل المستودع نفسه
ليبقى دائماً. مراحل HR‑1…HR‑4 لم يُعثر على أثرٍ لها في الكود أو الفروع أو التاريخ
(لا يوجد موديول موارد بشرية سابق)، لذا تُعدّ **غير منفَّذة بعد** في المستودع.
أُنجزت HR‑5 بنطاقها المحدَّد في طلب المهمة وفرعها: **نهاية الخدمة + حماية الأجور**.

---

## حالة المراحل

| المرحلة | الوصف | الحالة |
|---|---|---|
| HR‑1 | (غير معروفة — لا أثر في المستودع) | ⛔ غير منفَّذة |
| HR‑2 | (غير معروفة — لا أثر في المستودع) | ⛔ غير منفَّذة |
| HR‑3 | (غير معروفة — يُذكر أنها «دُمجت» لكن دون أثر في الكود) | ❓ يلزم تأكيد المالك |
| HR‑4 | (غير معروفة — لا أثر في المستودع) | ⛔ غير منفَّذة |
| **HR‑5** | **نهاية الخدمة (المواد 84/85/87) + حماية الأجور (مدد/WPS)** | ✅ **منجزة** |

> إذا كانت HR‑1…HR‑4 موجودة في فرع/مستودع آخر، يُرجى توجيهي إليه لأربط الاعتماديات.
> موديول HR‑5 الحالي مستقل ولا يفترض وجود تلك المراحل (يعتمد فقط على `rawasi_base` و`hr`).

---

## HR‑5 — ما أُنجز (موديول `rawasi_hr`)

### 1) مكافأة نهاية الخدمة — `rawasi.eos.settlement`
- احتساب آلي وفق **نظام العمل السعودي**:
  - **المادة 84:** نصف شهر عن كل سنة من السنوات الخمس الأولى، وشهر كامل عمّا بعدها،
    مع احتساب كسور السنة بالتناسب على أساس الأجر الأخير.
  - **المادة 85 (الاستقالة):** أقل من سنتين = لا شيء، 2–5 = الثلث، 5–10 = الثلثان،
    10 فأكثر = كامل.
  - **المادة 87 والحالات المماثلة:** استحقاق كامل (وفاة/عجز، إنهاء صاحب العمل،
    انتهاء عقد محدد المدة، حالة خاصة). والفصل النظامي (المادة 80) = صفر.
- وعاء الأجر يُجلب من تركيبة أجر الموظف (أساسي + سكن + بدلات أخرى).
- مستحقات إضافية (بدل إجازات، مستحقات أخرى) واستقطاعات → صافي مستحق.
- **قيد محاسبي**: مدين «مخصص نهاية الخدمة» / دائن «مستحقات الموظفين»
  (الحسابات واليومية تُضبط في إعدادات الشركة).
- دورة حالة: مسودة → معتمدة → مُرحَّلة محاسبياً → مدفوعة (+ إلغاء/إعادة لمسودة).

### 2) حماية الأجور — `rawasi.wps.batch` + `rawasi.wps.line`
- تجميع رواتب موظفي الشركة في دفعة شهرية، وتعبئة تلقائية من تركيبة الأجر.
- توليد **ملف SIF (CSV)** بصيغة مدد/WPS: سجل المنشأة + سجلات الموظفين
  (الآيبان، الأساسي، السكن، البدلات، الاستقطاعات، الصافي)، قابل للتنزيل.
- تحقّقات: وجود رقم المنشأة، ووجود آيبان لكل موظف.

### 3) توسعة الموظف — `hr.employee`
- تركيبة الأجر السعودية (أساسي/سكن/أخرى → إجمالي محسوب).
- بيانات حماية الأجور: رقم العامل في مكتب العمل، الآيبان، رمز البنك.

### 4) الأمان والإعدادات
- مجموعة **«الموارد البشرية»** (`group_rawasi_hr`) ضمن امتياز «رواسي»،
  ممنوحة لأدمن رواسي تلقائياً. وصول للقراءة لمدير الحسابات.
- إعدادات الشركة: رقم المنشأة، حساب بنك الرواتب، يومية وحسابات نهاية الخدمة
  (شاشة الإعدادات ← تطبيق «رواسي سما»).
- الحسابات المحاسبية لنهاية الخدمة موجودة سلفاً في شجرة `rawasi_base`
  (مثل `5211200` مخصص مكافأة نهاية الخدمة).

### 5) الاختبارات — `tests/test_hr5.py`
- 14 اختبار لاحتساب نهاية الخدمة (كل شرائح المادتين 84/85 + الحالات الخاصة + التحقق
  من التواريخ ودورة الحالة) و4 اختبارات لتوليد ملف حماية الأجور.

---

## التثبيت
```
موديول جديد: rawasi_hr  (يعتمد على rawasi_base, hr, account)
```
بعد التثبيت: اضبط في الإعدادات «رقم المنشأة» والحسابات/اليومية، ثم عرّف تركيبة
أجر الموظفين، فتظهر قائمتا «نهاية الخدمة» و«حماية الأجور» تحت «رواسي سما».

## ملاحظات وافتراضات تحتاج تأكيد المالك
1. **وعاء أجر نهاية الخدمة**: اعتُمد الأجر الشامل (أساسي + بدلات ثابتة)؛ بعض العقود
   تحسبها على الأساسي فقط. الحقل `wage_base` قابل للتعديل يدوياً لكل تسوية.
2. **صيغة ملف WPS**: أعمدة قياسية عامة؛ قد يتطلب بنك بعينه ترتيباً مختلفاً —
   يُضبط بسهولة في `wps_batch.py::action_generate_file`.
3. **HR‑1…HR‑4**: إن وُجدت فعلاً، يلزم رابطها لربط الاعتماديات (انظر أعلاه).

## الخطوات التالية المقترحة
- تأكيد الافتراضات أعلاه.
- (اختياري) مخصص شهري آلي لنهاية الخدمة عبر `ir.cron` (تكوين المخصص تدريجياً).
- (اختياري) ربط حماية الأجور بموديول الرواتب الرسمي `hr_payroll` إن كان مُفعَّلاً.
