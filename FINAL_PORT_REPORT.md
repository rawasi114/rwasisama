# تقرير الترحيل النهائي — رواسي سما (الفرع الجديد)

ترحيل ميزات الإنتاج من موديولي **المقاولات** و**الورشة** إلى الفرع الجديد، مع
اعتماد نسخة الفرع عند التعارض، وتبسيط ما كان يربك المستخدمين، والحفاظ على المنطق
الوظيفي. لم تُمسّ بيئة الإنتاج (`rawasisama`) ولا كلمات المرور إطلاقاً.

## ملخص المراحل

| المرحلة | المحتوى | الحالة |
|--------|---------|--------|
| A | التصنيفات (LCGPA/SBC) + تطبيع عربي | ✅ |
| B | ذاكرة الأسعار المؤسسية (Price Intelligence) | ✅ |
| C | استيراد جدول الكميات Excel + محرك المطابقة + سجل التدقيق | ✅ |
| D | لوحة ميزانية المشروع | ✅ |
| E | اعتماد المواد (MAS) | ✅ |
| F | مقاولو الباطن + العقود الفرعية + مستخلصات الباطن | ✅ |
| G | ربط الورشة (موجود مسبقاً في موديول التكامل) | ✅ |
| H | العرض الموحَّد للمشروع (أزرار ذكية لمهندس الموقع) | ✅ |
| I | الجدول الزمني + Gantt (WBS مبسَّط) | ✅ |
| J | ترحيل المستخدمين والبيانات (قوالب استيراد آمنة) | ✅ |

## أبرز قرارات التبسيط (مقارنةً بالإنتاج)
- **اعتماد المواد:** أُسقط `workflow.mixin` و`_ensure_admin`؛ تُدار الحالات عبر
  مجموعات الصلاحيات القياسية. دمج PDF صار زراً اختيارياً دون تقرير غلاف QWeb.
- **مقاول الباطن والمستخلص:** نفس منطق المحتجز/الضريبة/التراكمي، بلا mixins.
- **WBS:** حُذف المسار الحرج وخط الأساس والطفو الكلي (كانت تربك ونادراً ما تُحدَّث)؛
  بقيت شجرة الأنشطة + التواريخ + الأنشطة السابقة + نسبة الإنجاز + Gantt.
- **محرك المطابقة:** أربعة مستويات (نص مطبَّع → رمز LCGPA → تقريبي → بلا) دون
  اعتماد قاعدة-بيانات إضافية (pgvector)، ولا اعتماد تلقائي — مراجعة بشرية دائماً.

## النماذج المضافة في موديول المقاولات
`rawasi.lcgpa.code`، `rawasi.sbc.code`، `rawasi.price.intelligence`،
`rawasi.item.variant`، `rawasi.import.audit`، `rawasi.import.batch(.line)`،
`rawasi.material.approval`، `rawasi.subcontractor`، `rawasi.subcontract(.line)`،
`rawasi.subcontract.ipc(.line)`، `rawasi.wbs.activity`.

## التحقق الآلي (محلي — بلا أودو)
- جميع ملفات Python تُترجم بنجاح (`ast.parse`).
- جميع ملفات XML سليمة (`minidom.parse`).
- `ir.model.access.csv`: 117 صفاً، 8 أعمدة لكل صف، وكل مرجع موديل معرَّف محلياً.
- لا تعارض في معرّفات XML، وكل ملفات المانيفست موجودة.

## الاختبارات (تُنفَّذ على بناء Odoo.sh عند الدفع)
ملفات اختبار جديدة: المراحل D، E، F، H، I (إضافةً إلى A، B، C السابقة). تغطي:
تسلسلات الترقيم، سير الحالات، حسابات المحتجز/الضريبة/التراكمي، قيود التجاوز،
حسابات الميزانية والتكلفة المخططة، وعدّادات الأزرار الذكية.

## الخطوة التالية اليدوية (المسؤول)
1. دفع الفرع لبناء Odoo.sh وتشغيل الاختبارات.
2. ترحيل المستخدمين والبيانات عبر قوالب `migration/` (راجع `migration/README.md`).
3. بعد نجاح البناء واعتماد القبول، الترقية إلى الإنتاج.

> لم يُنفَّذ أي `git commit` — التغييرات جاهزة للمراجعة والدفع بقرار منك.
