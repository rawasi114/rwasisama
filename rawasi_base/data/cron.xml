<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- تصعيد الاعتمادات/النشاطات المتأخرة — يومياً -->
        <record id="cron_rawasi_escalate_overdue" model="ir.cron">
            <field name="name">رواسي: تصعيد الاعتمادات المتأخرة</field>
            <field name="model_id" ref="model_rawasi_approval_chain"/>
            <field name="state">code</field>
            <field name="code">model._cron_escalate_overdue()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active" eval="True"/>
        </record>

    </data>
</odoo>
