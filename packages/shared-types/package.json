{
  "name": "@rawasi/shared-types",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"(no lint configured for shared-types)\"",
    "test": "echo \"(no tests for shared-types)\""
  },
  "devDependencies": {
    "typescript": "^5.5.3"
  }
}
