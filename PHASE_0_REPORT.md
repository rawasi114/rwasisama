# PHASE_0_REPORT.md — تقرير المرحلة صفر (التأسيس)

> **الحالة:** مكتملة ✅ — بانتظار موافقتكم للانتقال إلى المرحلة الأولى.
> **الفرع:** `claude/keen-gates-KKqLj`
> **المرجع:** `SPEC.md` §8 (المرحلة صفر)، `PLAN.md` §11.

---

## 1. ملخص تنفيذي

تم تأسيس البنية الكاملة للمشروع: monorepo يعمل، قاعدة بيانات بأول migration، نظام مصادقة
وصلاحيات (Auth + RBAC) مُختبَر فعلياً، نظام ملفات، تدقيق (Audit)، تسجيل (Logging)، وواجهة
أمامية بدعم RTL كامل فيها صفحة دخول ولوحة فارغة. **مخرج المرحلة المطلوب** ("نظام يمكن
تسجيل الدخول إليه بمستخدمين بأدوار مختلفة، مع لوحة فارغة") **تحقّق وتم التحقق منه.**

---

## 2. قائمة التسليم مقابل متطلبات الدستور (§8)

| متطلب المرحلة صفر | الحالة | التفاصيل |
|---|---|---|
| إعداد البنية (Monorepo) | ✅ | pnpm workspaces + Turborepo (`apps/api`, `apps/web`) |
| Docker للتطوير | ✅ | `docker-compose.yml`: PostgreSQL 16 + Redis 7 + MinIO |
| CI/CD | ✅ | `.github/workflows/ci.yml`: install → generate → migrate → lint → build → test |
| قاعدة البيانات + أول Migration | ✅ | `apps/api/prisma/migrations/…_init` مطبّق على Postgres |
| المصادقة (Auth) | ✅ | JWT (access 15د) + Refresh Tokens مع تدوير وإبطال مُخزّن مجزّأً |
| الصلاحيات (RBAC) | ✅ | CASL — حارس صلاحيات عام، ٧ أدوار، ١٤٤ صلاحية، مُختبَر |
| نظام الملفات (Files) | ✅ | تجريد تخزين + سائق محلي، رفع/تنزيل، ربط polymorphic جاهز |
| التسجيل والتدقيق (Logging + Audit) | ✅ | Pino (correlation IDs) + جدول `audit_logs` وخدمة تسجيل |
| صفحة Login + Dashboard فارغة + RTL | ✅ | Next.js App Router، `dir="rtl"`، هوية رواسي سما |

**جداول قاعدة البيانات (المرحلة صفر):** `users`, `roles`, `permissions`, `refresh_tokens`,
`files`, `file_links`, `audit_logs`, `units_lookup`, `sbc_codes_lookup`.
(جداول `boq_items` والمجال المحوري تبدأ في المرحلة الأولى.)

**البيانات الأولية (Seed):** ١٣ وحدة موحّدة مع كل الكتابات المُلاحظة في ملفّي اعتماد،
٥٠ رمز SBC، ٧ أدوار بصلاحياتها، ومستخدم admin.

---

## 3. دليل التحقق (Verification Evidence)

تم التحقق فعلياً مقابل قاعدة بيانات Postgres حيّة وخادم API يعمل:

```
# فحص الصحة
GET /api/health            → {"status":"ok","db":true}

# تسجيل دخول admin
POST /api/auth/login       → accessToken + refreshToken + user
GET  /api/auth/me          → role: CEO (المدير العام) | permissions: 144

# قائمة الأدوار (تتطلب role.read)
GET  /api/roles            → 7 أدوار (CEO, PROJECTS_DIRECTOR, ... )

# الأمان
GET  /api/roles (بدون توكن) → HTTP 401

# اختبار RBAC الحاسم
admin ينشئ مستخدم SITE_ENGINEER → نجح
site engineer: 16 صلاحية | material_request.create = true | user.read = false
GET /api/users (كـ site engineer) → HTTP 403
  message: لا تملك صلاحية "read" على "user"

# نظام الملفات
POST /api/files (رفع) → fileId
GET  /api/files/:id/download → استرجاع المحتوى الأصلي ✅
```

**الواجهة الأمامية:**
```
next build → نجح، كل المسارات مُولّدة مسبقاً (/, /login, /dashboard)
GET /login → HTTP 200، يحتوي lang="ar" + dir="rtl" + الواجهة العربية
GET /      → 307 إعادة توجيه إلى /login
```

**فحص الـ monorepo الكامل (مطابق لـ CI):** `pnpm lint` ✅ · `pnpm build` ✅ · `pnpm test` ✅ (اختباران ناجحان).

---

## 4. قرارات وانحرافات (مبرّرة)

1. **مكان Prisma:** وُضع داخل `apps/api/prisma` (لا في `packages/`) لتفادي هشاشة توليد عميل
   Prisma عبر حدود الحزم في pnpm. الواجهة لا تحتاج Prisma (تتصل عبر REST). انحراف طفيف عن
   مخطط المجلدات في §7 لصالح الموثوقية.
2. **bcryptjs بدل bcrypt:** خوارزمية bcrypt نفسها لكن تنفيذ JS خالص (يتجنّب فشل البناء الأصلي
   في البيئات بلا أدوات بناء C). cost factor = ١٢ كما يطلب الدستور.
3. **Lint = `tsc --noEmit` مؤقتاً:** فحص أنواع موثوق وأخضر؛ إضافة ESLint الكامل ضمن تحسين لاحق.
4. **سائق التخزين المحلي افتراضياً:** يفي بنظام الملفات للمرحلة صفر دون الحاجة لتشغيل MinIO؛
   سائق S3 يُفعّل في المرحلة الرابعة (DMS/MAS).
5. **الخط:** استُخدم مجموعة خطوط نظامية تدعم العربية مؤقتاً (DIN Next LT Arabic يتطلب ملفات
   الخط المرخّصة — السؤال المفتوح #6).
6. **الإعدادات الافتراضية المعتمدة منكم:** مصادقة محلية (JWT)، REST + OpenAPI.

---

## 5. ما هو خارج نطاق المرحلة صفر (مؤجّل عمداً)

- جداول ومنطق BOQ والمجال المحوري (المرحلة الأولى).
- الاستيراد الذكي لقالب اعتماد (المرحلة الأولى).
- `packages/shared-types` و `packages/ui` (تُضاف عند وجود أنواع/مكونات مشتركة فعلية).
- تكامل Redis/BullMQ الفعلي (يبدأ مع الاستيراد كـ Background Job في المرحلة الأولى).
- ESLint الكامل، اختبارات E2E بالمتصفح، سائق S3.

> ملاحظة شفافية: التحقق من الواجهة تم على مستوى البناء و SSR (RTL/عربي) و API الذي تعتمد
> عليه، **دون** نقر فعلي في متصفح (لا يتوفّر متصفح في بيئة التنفيذ الحالية).

---

## 6. أسئلة مفتوحة لا تزال قائمة (تؤثر على المراحل القادمة)

من `PLAN.md` §10 — أهمها قبل/أثناء المراحل القادمة:
- **#6 خطوط الهوية + #7 شعار/قالب MAS:** مطلوبة للمرحلة الرابعة (دمج PDF لاعتمادات المواد).
- **#9 قائمة SBC الكاملة:** نكتفي بالمُلاحظ ونوسّع، أم تزوّدوننا بالقائمة الرسمية؟
- **#1 الاستضافة/سيادة البيانات، #2 نفاذ (Nafath):** تؤثر على مرحلة النشر والمصادقة المتقدمة.
- **#4 مكتبة Gantt:** مطلوبة للمرحلة الثانية.

(بقية الأسئلة لا تُعطّل المرحلة الأولى.)

---

## 7. الخطوة التالية (المرحلة الأولى — بانتظار موافقتكم)

عند موافقتكم، أبدأ **المرحلة الأولى: إدارة المنافسات والمشاريع**:
1. جداول `opportunities`, `projects`, `boq_sections?`, **`boq_items`** (الكيان المحوري) في Prisma.
2. **الاستيراد الذكي لقالب اعتماد** (Header-Based) — يعمل على Variant A و B بنفس الكود، مع
   خدمات `UnitsNormalizationService` و `SbcCodesService`، كـ Background Job مع تقرير استيراد.
3. CRUD للفرص، شاشة التسعير (بلا الذكاء التسعيري)، التكاليف غير المباشرة، معالج التحويل لمشروع.
4. اختبارات وحدة وتكامل تشمل الـ ١٠ حالات الحافة ومعايير القبول في الملحق أ.٩.

> **متوقّف الآن بانتظار:** اعتماد المرحلة صفر، والإجابة على الأسئلة المفتوحة المؤثّرة (القسم ٦).

— Claude Code
