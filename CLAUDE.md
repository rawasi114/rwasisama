# تعليمات دائمة — رواسي سما (CLAUDE.md)

> هذا الملف يُقرأ تلقائياً في بداية كل جلسة. التعليمات هنا **إلزامية وعبر كل الجلسات**.

## اللغة
- التواصل **بالعربية** حيثما أمكن.

## بيئة Odoo المستهدفة (إلزامي)
- العمل **دائماً** على قاعدة بيانات **الإنتاج**: **`RawasiSamaCO`**
- العنوان: `https://rawasisama.odoo.com` — الشركة: «شركة رواسي سما للمقاولات»
- الوصول عبر خادم **Odoo MCP** المربوط بالجلسة.

## قاعدة السلامة على الإنتاج (مهمة)
بما أن `RawasiSamaCO` قاعدة **إنتاجية حيّة**، فأي عملية تؤثّر على بيانات فعلية:

- **القراءة/الاستعلام** (search_read, read_record, read_group, list_models,
  get_model_schema, code_read/search...): **مسموحة بحرية**.
- **الكتابة/التعديل/الحذف** (create_record, update_record, delete_record,
  وأي `execute_method` يغيّر بيانات، وتثبيت/تحديث الموديولات عبر manage_module):
  **لا تُنفَّذ إلا بطلب صريح من المستخدم**، مع تأكيد العملية ونطاقها قبل التنفيذ.

## مستودع الكود
- التطوير على الفروع المخصّصة، والدمج في فرع `rawasisama` (فرع التكامل).
- موديولات النظام: `rawasi_base`, `rawasi_hr`, `rawasi_construction`,
  `rawasi_workshop`, `rawasi_integration` ... إلخ.
- ملف `HANDOFF.md` يتتبّع حالة المراحل — يُحدَّث عند نهاية كل مرحلة.
