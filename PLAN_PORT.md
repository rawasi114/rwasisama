# خطة نقل ميزات البرودكشن إلى الفرع — `claude/wizardly-cerf-HRAUG`

> الحالة: **مسودة بانتظار اعتمادك**. لا كود قبل موافقتك.
> النطاق: الخيار (ب) + متطلباتك الإضافية (جداول زمنية/جانت، ميزانية، شاشة مشروع موحّدة، نقل بيانات).

---

## 1. الواقع المعماري (مهم)
الفرع إعادة بناء نظيفة (4 موديولات: `rawasi_base` + construction + workshop + integration). النقل **تكييف** لا نسخ.

| العنصر | البرودكشن | الفرع | الأثر |
|---|---|---|---|
| `boq.item` | غني | ✅ موجود، أسماء حقول مختلفة | تعيين + إضافة 3 حقول |
| البند المرجعي | غني (lcgpa/variants/specs) | مبسّط | إثراء |
| سجل التدقيق | حقول استيراد خاصة | مخطط مختلف | إعادة كتابة التسجيل |
| حُرّاس الأدوار | `workflow.mixin`+`_ensure_*` | مجموعات + سلاسل اعتماد | إعادة ربط بالمجموعات |
| أمر شراء | `rawasi.purchase.order` مخصّص | قياسي (`stock.picking`) | استلام قياسي |
| التقارير اليومية | `rawasi.daily.report` | ✅ موجودة `rawasi.dsr` | لا نقل — ربط فقط |
| Gantt | `<gantt>` (Enterprise) | متاح (Odoo.sh = Enterprise) ✅ | نقل WBS مبسّط |

تعيين الأدوار: تقني→`group_technical_office` · مالي→`group_rawasi_accountant` · أدمن→`group_rawasi_admin`.

---

## 2. القرارات (محدّثة بردودك)
1. **استلام المواد:** استلام أودو القياسي (`stock.picking`) — لا نظام شراء مخصّص.
2. **تدقيق الاستيراد:** جدول تدقيق استيراد **مخصّص خفيف**.
3. **اعتماد المواد:** دمج PDF التلقائي **اختياري** (زر منفصل).
4. **نقل البيانات:** **سكربت تحويل** يقرأ تصدير الإنتاج (قراءة فقط) ويحوّله للموديلات الجديدة.
5. **المستخدمون:** أقل من 10، يُنشأون بكلمات مرور مؤقتة يغيّرها الموظف عند أول دخول. **لن ألمس كلمات مرور الإنتاج.**
6. **الترتيب:** **الجدول الزمني/Gantt مؤجّل للأخير**؛ نبدأ بالأهم.
7. **الورشة:** نُبقي ورشة الفرع المبسّطة؛ لا نقل للنماذج التشغيلية الثقيلة إلا ما يُطلب صراحةً *(بانتظار جوابك: جودة/صيانة/HSE؟)*.

---

## 3. أعمدة استيراد جدول الكميات (كما طلبت بالضبط)
| العمود المطلوب | حقل الفرع | ملاحظة |
|---|---|---|
| الرقم التسلسلي | `serial` | **إضافة** |
| الفئة | `category` | **إضافة** |
| الوصف | `description` | موجود |
| المواصفات | `specification` | موجود |
| الكمية | `qty` | موجود |
| وحدة القياس | `uom_id` | موجود |
| التكلفة الإفرادي | `cost_estimate` | موجود |
| إجمالي التكلفة للبند | `total_cost` = الكمية×التكلفة | **إضافة (محسوب)** |
| السعر الإفرادي | `unit_price` | موجود |
| إجمالي البند | `subtotal` = الكمية×السعر | موجود |

الاستيراد إلزامي الربط بمنافسة (`rawasi.competition`). معالج رفع Excel → معاينة/مطابقة → مراجعة بشرية → ترحيل البنود للمنافسة.

---

## 4. التسليم على مراحل (الترتيب الجديد — Gantt مؤجّل للأخير)
بوابة اعتماد + اختبارات ≥80% + تقرير بعد كل مرحلة.

**A — التصنيفات + إثراء البند المرجعي + حقول BOQ** *(الأساس — يبدأ الآن)*
LCGPA + SBC (بذر 65 رمزاً من `lcgpa_codes_seed.csv`)؛ إضافة `lcgpa_code_id`/`sbc_code_id` للبند المرجعي؛ إضافة `serial`/`category`/`total_cost` لـ boq.item. *(حقول `variant_ids`/`extracted_spec_ids` تُضاف في C مع موديلاتها.)*

**B — ذاكرة التسعير** (`rawasi.price.intelligence`، مطابقة فجوية بلا pgvector) + لوحة «أسعار تاريخية مشابهة».

**C — الاستيراد + محرك المطابقة** (`item.variant` + `extracted.specification` + `import.batch`/line + معالج Excel + تدقيق استيراد مخصّص). 4 مستويات (نص/LCGPA/نمط/لا تطابق). **مراجعة بشرية دائماً، لا اعتماد تلقائي.** الأعمدة العشرة كما في القسم 3.

**D — لوحة ميزانية المشروع** *(طلبك)*
تعاقدي مقابل: تكلفة مقدّرة (BOQ) + ملتزَم (مواد/باطن) + مصروف فعلي + متبقّي + نسبة استهلاك + تنبيه تجاوز.

**E — اعتماد المواد** (`material.approval`، حالات A/B/C/D، دمج PDF اختياري، حارس تقني).

**F — المقاولون من الباطن** (`subcontractor` + `subcontract` + `subcontract.ipc` يحاكي نمط `rawasi.ipc`؛ محتجز/ضريبة 15%/تراكمي ≤ تعاقدي).

**G — الورشة** *(طلبك «لا تنسَ الورشة»)*
إبقاء ورشة الفرع المبسّطة (أقسام/MO/طلبات مواد/دفع/بيع) + ربطها بالشاشة الموحّدة. إضافة نماذج تشغيلية ثقيلة (جودة/صيانة/HSE) **فقط إن طلبتها** — *بانتظار جوابك*.

**H — شاشة المشروع الموحّدة لمهندس الموقع** *(طلبك)*
أزرار ذكية/تبويبات داخل المشروع تجمع: مستندات · اعتماد مواد · تقارير يومية (DSR) · أوامر تغيير · مستخلصات · عقود باطن · ميزانية.

**I — الجداول الزمنية + Gantt** *(مؤجّل للأخير بطلبك)*
WBS مبسّط: مراحل + أنشطة (تواريخ/مدة/تبعيات/إنجاز/معالم) + Gantt + ربط بـ boq.item. تعقيدات متقدّمة (مسار حرج/خط أساس/طفو) مؤجّلة/مخفية.

**J — نقل المستخدمين والبيانات** (سكربت تحويل — انظر القسم 5).

---

## 5. نقل المستخدمين والبيانات — الواقع والخيارات (يحتاج قرارك)

**المشكلة:** فرع التطوير يُبنى ببيانات تجريبية، لا ببيانات الإنتاج. وبيانات الإنتاج محفوظة في **موديلات قديمة مختلفة** عن موديلات الفرع الجديد، فلا يمكن «نسخها» مباشرة.

**المستخدمون** (`res.users`/`res.partner`): موديلات قياسية متطابقة → قابلة للنقل بسهولة. **لن ألمس كلمات المرور** — تُضبط بإعادة تعيين أو يحددها الأدمن.

**بيانات الأعمال** (منافسات/مشاريع/BOQ…): تحتاج **تحويلاً** للموديلات الجديدة. وبما أنها «صغيرة» كما ذكرت، فالخياران:

- **خيار 1 (موصى به للبيانات الصغيرة):** بعد تثبيت الموديولات، أُنشئ المستخدمين + أُدخل البيانات الصغيرة يدوياً/بقالب استيراد. آمن، نظيف، بلا سكربتات ترحيل هشّة.
- **خيار 2:** سكربت تحويل يقرأ تصدير الإنتاج (قراءة فقط، لا مساس بالإنتاج) ويحوّله للموديلات الجديدة. أنسب لو البيانات أكثر مما تتوقع.

> ملاحظة استراتيجية: حين تعتمد هذا الفرع ليصير إنتاجاً، سيُطبَّق على قاعدة بيانات الإنتاج الحالية → يلزم ترحيل بيانات حقيقي. كون البيانات صغيرة الآن **يجعل الإدخال اليدوي أأمن** من سكربتات الترحيل. سنخطط لهذا عند الاعتماد النهائي.

---

## 6. ما نُسقطه / لا نمسّه
نُسقط: pgvector، نظام الشراء المخصّص، نماذج الورشة الثقيلة (بطاقات وقت/جودة/HSE/صيانة/إغلاق)، تعقيدات WBS المتقدمة (مؤجّلة).
لا نمسّ: فرع الإنتاج `rawasisama`، أي موديول خارج base/construction.

## 7. الأعراف
مصطلحات معتمدة حرفياً · كود إنجليزي/واجهة عربية RTL · اختبارات ≥80% · Conventional Commits · ستيج/تطوير فقط · لا مفاتيح سرية.

---

## 8. أحتاج ردّك على هذه فقط قبل البدء
1. **نقل البيانات:** خيار 1 (إدخال يدوي/قالب — موصى به) أم خيار 2 (سكربت تحويل)؟
2. **المستخدمون:** أنشئهم الآن بكلمات مرور مؤقتة يغيّرها الموظف عند أول دخول؟ (كم عددهم تقريباً وأدوارهم؟)
3. **الترتيب:** أبدأ بالمرحلة A كما رتّبت، أم تريد تقديم الجداول الزمنية (D) أو الاستيراد (C)؟

> بعد ردّك أبدأ مرحلة واحدة فقط وأقدّم تقريرها لاعتمادك قبل التالية.
