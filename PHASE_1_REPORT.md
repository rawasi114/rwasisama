# تقرير المرحلة الأولى — المنافسات والاستيراد الذكي والتسعير

> **الحالة:** ✅ مكتملة ومُتحقَّق منها على **Odoo 19** (تثبيت + ١٥ اختبار تمر).
> **بانتظار موافقتك قبل الانتقال للمرحلة الثانية.**

---

## ١. ما تم بناؤه

### الموديلات (Models)
| الموديل | الوصف |
|---|---|
| `rawasi.competition` | المنافسة/الفرصة — مع سير حالات (مسودة → تسعير → مقدَّم → فائز/خاسر)، ملخص تسعير، chatter |
| `rawasi.boq.item` | **بند جدول الكميات (النواة الذرية)** — وصف، فئة، وحدة، كمية، تكلفة/سعر، رمز SBC |
| `rawasi.indirect.cost` | تكاليف غير مباشرة (نسبة من المباشرة أو مبلغ ثابت) |
| `rawasi.unit` | وحدات قياس مع **مرادفات للتطبيع** (يحل تعدد كتابة الوحدات) |
| `rawasi.sbc.code` | رموز كود البناء السعودي (SBC) |

### الاستيراد الذكي (`rawasi.boq.import.wizard`) — الميزة المحورية
- **مطابقة بعناوين الأعمدة** (Header-Based) بعد تطبيع عربي خفيف — لا يعتمد على ترتيب الأعمدة.
- **Forward-Fill** للفئة والمجموعة (الخلايا الفارغة ترث آخر قيمة).
- **تطبيع وحدات القياس** ومطابقتها بجدول المرادفات (كل أشكال «م.ط/م ط/م/ط» تُوحَّد).
- **ربط رموز SBC** تلقائياً، مع تجاهل «غير محدد».
- **رفض الملفات غير المطابقة** لجدول الكميات (مع رسالة واضحة).
- تقرير تحذيرات (وحدات غير معروفة، كميات غير صالحة، محتوى محلي فارغ، رموز غير مطابقة).

### التسعير والتحويل
- حساب تلقائي: التكلفة المباشرة، غير المباشرة، إجمالي التكلفة، سعر العرض، هامش الربح %.
- زر **«تطبيق الهامش الافتراضي»** على كل البنود.
- زر **«تحويل لمشروع»** (يُنشئ `project.project` ويربطه) بعد الترسية.

### الواجهات والأمان
- قائمة + نموذج المنافسة (بنود BOQ inline، تكاليف غير مباشرة، ملخص تسعير، أزرار سير العمل).
- شاشات إعداد (وحدات، رموز SBC) + قوائم تحت «رواسي سما».
- صلاحqيات (ir.model.access) للأدوار السبعة (محرِّرون: التخطيط/المكتب الفني/مدير المشاريع؛ قراءة: المحاسب/الموقع/الاستشاري؛ المدير العام يرث الكل).

---

## ٢. التحقق الفعلي (Odoo 19)

تثبيت الموديول مع تبعية `project`، وتشغيل اختبارات الموديول:

```
0 failed, 0 error(s) of 15 tests   ✅
```

**تغطية الاختبارات (١٥ اختبار):**
- **الاستيراد (٦):** Variant A (٣٩ بند، تطبيع م²)، Variant B (٢٠٩ بند، Forward-Fill
  للفئة، ربط SBC «2002»، وصف طويل ١١٠٠+ حرف، تطبيع م.ط)، **رفض الملف غير الصالح**،
  الاستبدال، نقل الحالة للتسعير.
- **التسعير (٥):** إجماليات البنود والمنافسة، تكلفة غير مباشرة (نسبة + ثابت)،
  تطبيق الهامش، منع التقديم بلا بنود، التحويل لمشروع يتطلب الترسية.
- **الأمان (٤):** الأدوار السبعة (من المرحلة صفر).

**أخطاء اكتشفها التشغيل الفعلي وصُحّحت قبل التسليم:**
1. تغيّر صيغة عرض البحث في Odoo 19 (لا `expand`/`string` على `group` التجميع).
2. **تبعية حسابية دائرية** بين التكلفة غير المباشرة وإجمالي التكلفة المباشرة —
   حُلّت بحساب الأساس مباشرةً من بنود BOQ.

---

## ٣. ملاحظات

- الذكاء التسعيري (حفظ الأسعار في ذاكرة مؤسسية) مجدوَل للمرحلة الخامسة كما في الخطة؛
  هنا أسّسنا بنية البنود والتسعير التي سيُبنى عليها.
- لوحة التحكم تبقى placeholder؛ مؤشرات الأداء تُبنى في مرحلة لوحات المعلومات.

---

## ٤. الخطوة التالية

بانتظار موافقتك للانتقال إلى **المرحلة الثانية** (هيكل تجزئة العمل WBS بالمراحل
الخمس + ربط BOQ بالمراحل + Gantt عبر `web_gantt` من Enterprise + استيراد الجدول الزمني).
