<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_work_order_list" model="ir.ui.view">
        <field name="name">rwasi.work.order.list</field>
        <field name="model">rwasi.work.order</field>
        <field name="arch" type="xml">
            <list string="أوامر التصنيع" decoration-info="state == 'confirmed'" decoration-success="state == 'done'" decoration-muted="state == 'cancel'">
                <field name="name"/>
                <field name="sale_order_id"/>
                <field name="partner_id"/>
                <field name="product_id"/>
                <field name="product_qty"/>
                <field name="workshop_scope"/>
                <field name="required_date"/>
                <field name="payment_status" widget="badge"
                       decoration-success="payment_status == 'paid'"
                       decoration-warning="payment_status == 'partial'"
                       decoration-danger="payment_status == 'not_paid'"/>
                <field name="state" widget="badge"/>
            </list>
        </field>
    </record>

    <record id="view_work_order_form" model="ir.ui.view">
        <field name="name">rwasi.work.order.form</field>
        <field name="model">rwasi.work.order</field>
        <field name="arch" type="xml">
            <form string="أمر تشغيل / تصنيع">
                <header>
                    <button name="action_submit_materials" string="طلب اعتماد المواد" type="object"
                            class="btn-primary"
                            invisible="state != 'draft' or materials_available or not material_line_ids or paid_ratio &lt; 0.5 or material_approval_state in ('to_approve','approved')"/>
                    <button name="action_approve_materials" string="اعتماد المواد" type="object"
                            class="btn-primary"
                            invisible="material_approval_state != 'to_approve' or not can_approve_materials"/>
                    <button name="action_reject_materials" string="رفض" type="object"
                            invisible="material_approval_state != 'to_approve' or not can_approve_materials"/>
                    <button name="action_revise_materials" string="إعادة للتعديل" type="object"
                            invisible="material_approval_state != 'to_approve' or not can_approve_materials"/>
                    <button name="action_handover_materials" string="تسليم المواد للورشة" type="object"
                            class="btn-primary"
                            invisible="material_approval_state != 'approved' or material_handover_done or not can_handle_purchase"/>
                    <button name="action_confirm" string="تأكيد التصنيع" type="object"
                            class="btn-primary"
                            invisible="state != 'draft'"/>
                    <button name="action_start_production" string="بدء التصنيع" type="object"
                            class="btn-primary"
                            invisible="state != 'confirmed' or paid_ratio &lt; 0.5"/>
                    <button name="action_finish_production" string="إنهاء التصنيع" type="object"
                            class="btn-primary" invisible="state != 'in_production'"/>
                    <button name="action_deliver" string="التسليم للعميل" type="object"
                            class="btn-primary"
                            invisible="state != 'finished' or payment_status != 'paid'"/>
                    <button name="action_close" string="إغلاق" type="object"
                            invisible="state != 'delivered'"/>
                    <button name="action_draft" string="إعادة لمسودة" type="object"
                            invisible="state in ('draft', 'cancel')"/>
                    <button name="action_cancel" string="إلغاء" type="object"
                            invisible="state in ('done', 'cancel')"/>
                    <field name="state" widget="statusbar"
                           statusbar_visible="draft,confirmed,in_production,finished,delivered,done"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_purchase_orders" type="object"
                                class="oe_stat_button" icon="fa-shopping-cart"
                                invisible="purchase_order_count == 0">
                            <field name="purchase_order_count" widget="statinfo" string="طلبات الشراء"/>
                        </button>
                        <button name="action_view_delivery" type="object"
                                class="oe_stat_button" icon="fa-truck"
                                invisible="delivery_count == 0">
                            <field name="delivery_count" widget="statinfo" string="أمر التسليم"/>
                        </button>
                        <button name="action_view_closeout" type="object"
                                class="oe_stat_button" icon="fa-flag-checkered"
                                invisible="closeout_count == 0">
                            <field name="closeout_count" widget="statinfo" string="إغلاق المشروع"/>
                        </button>
                    </div>
                    <field name="has_draft_rfq" invisible="1"/>
                    <field name="has_rfq_to_confirm" invisible="1"/>
                    <field name="has_pending_receipt" invisible="1"/>
                    <field name="paid_ratio" invisible="1"/>
                    <field name="material_approval_state" invisible="1"/>
                    <field name="can_approve_materials" invisible="1"/>
                    <field name="can_handle_purchase" invisible="1"/>
                    <field name="material_handover_done" invisible="1"/>
                    <div class="alert alert-info" role="alert"
                         invisible="state != 'draft' or paid_ratio &gt;= 0.5">
                        بانتظار سداد دفعة لا تقل عن 50% من قيمة الطلب لتتمكّن من طلب شراء المواد.
                    </div>
                    <div class="alert alert-info" role="alert"
                         invisible="material_approval_state != 'to_approve'">
                        طلب المواد بانتظار اعتماد المسؤول.
                    </div>
                    <div class="alert alert-success" role="alert"
                         invisible="material_approval_state != 'approved' or material_handover_done">
                        تم اعتماد المواد وإسنادها لموظف المشتريات؛ بانتظار الشراء ثم تسليمها للورشة.
                    </div>
                    <div class="alert alert-danger" role="alert"
                         invisible="material_approval_state != 'rejected'">
                        تم رفض طلب المواد (انظر سبب الرفض / الإعادة أدناه).
                    </div>
                    <div class="alert alert-warning" role="alert"
                         invisible="material_approval_state != 'to_revise'">
                        أُعيد طلب المواد للتعديل (انظر سبب الرفض / الإعادة أدناه).
                    </div>
                    <group invisible="material_approval_state == 'draft' and not material_handover_done">
                        <group string="إسناد طلب المواد">
                            <field name="material_approver_assignee_id"
                                   readonly="material_approval_state not in ('draft','to_revise','rejected')"/>
                            <field name="purchase_user_id"
                                   readonly="material_approval_state not in ('to_approve',)"/>
                            <field name="material_approver_id" readonly="1"
                                   invisible="material_approval_state in ('draft','to_approve')"/>
                        </group>
                        <group string="سبب الرفض / الإعادة"
                               invisible="material_approval_state not in ('to_approve','rejected','to_revise')">
                            <field name="material_approval_reason" nolabel="1"
                                   readonly="material_approval_state != 'to_approve'"
                                   placeholder="يكتبه المسؤول عند الرفض أو الإعادة..."/>
                        </group>
                    </group>
                    <group string="تسليم المواد للورشة"
                           invisible="material_approval_state != 'approved'">
                        <field name="material_received_by"
                               readonly="material_handover_done"/>
                        <field name="material_handover_signature" widget="signature"
                               readonly="material_handover_done"/>
                        <field name="material_handover_date" readonly="1"
                               invisible="not material_handover_done"/>
                    </group>
                    <div class="alert alert-warning" role="alert"
                         invisible="state != 'confirmed' or paid_ratio &gt;= 0.5">
                        لا يمكن بدء التصنيع قبل سداد دفعة أولى لا تقل عن 50% من قيمة الفاتورة.
                    </div>
                    <div class="alert alert-warning" role="alert"
                         invisible="state != 'finished' or payment_status == 'paid'">
                        بانتظار سداد كامل قيمة العمل قبل الانتقال لمرحلة التسليم.
                        تتغيّر الحالة تلقائياً عند إغلاق الفاتورة من المحاسبة.
                    </div>
                    <div class="oe_title">
                        <h1><field name="name" readonly="1"/></h1>
                    </div>
                    <group>
                        <group string="بيانات الأمر">
                            <field name="sale_order_id" readonly="1"/>
                            <field name="partner_id"/>
                            <field name="product_id"/>
                            <field name="product_qty"/>
                            <field name="workshop_scope"/>
                            <field name="priority"/>
                        </group>
                        <group string="المشروع والمواعيد">
                            <field name="project_ref"/>
                            <field name="project_code"/>
                            <field name="date_issued"/>
                            <field name="start_date"/>
                            <field name="required_date"/>
                            <field name="est_duration"/>
                            <field name="drawing_ref"/>
                            <field name="materials_available" readonly="1"/>
                            <field name="payment_status" widget="badge"
                                   decoration-success="payment_status == 'paid'"
                                   decoration-warning="payment_status == 'partial'"
                                   decoration-danger="payment_status == 'not_paid'"/>
                        </group>
                    </group>
                    <field name="scope_of_work" placeholder="وصف العمل المطلوب..."/>
                    <notebook>
                        <page string="المواد اللازمة" name="materials">
                            <field name="material_line_ids" readonly="state != 'draft'">
                                <list editable="bottom" decoration-danger="not is_available" decoration-success="is_available">
                                    <field name="material_id"/>
                                    <field name="vendor_id"/>
                                    <field name="qty_needed"/>
                                    <field name="qty_available"/>
                                    <field name="uom_name"/>
                                    <field name="is_available"/>
                                </list>
                            </field>
                            <separator string="طلبات عرض السعر / الشراء"/>
                            <field name="purchase_order_ids" readonly="1">
                                <list>
                                    <field name="name"/>
                                    <field name="partner_id"/>
                                    <field name="amount_total"/>
                                    <field name="state"/>
                                </list>
                            </field>
                        </page>
                        <page string="بنود التنفيذ" name="lines">
                            <field name="line_ids">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="description"/>
                                    <field name="qty"/>
                                    <field name="uom"/>
                                    <field name="item_ref"/>
                                    <field name="status"/>
                                </list>
                            </field>
                        </page>
                        <page string="المتطلبات والاشتراطات" name="requirements">
                            <group>
                                <group>
                                    <field name="req_approved_dwgs"/>
                                    <field name="req_sample_approved"/>
                                    <field name="req_material_approval"/>
                                </group>
                                <group>
                                    <field name="req_third_party_test"/>
                                    <field name="req_site_install"/>
                                    <field name="req_special_packing"/>
                                </group>
                            </group>
                        </page>
                        <page string="التسليم وتوقيع العميل" name="delivery">
                            <group>
                                <group>
                                    <field name="customer_sign_name"/>
                                    <field name="customer_sign_date"/>
                                </group>
                                <group>
                                    <field name="customer_signature" widget="signature"/>
                                </group>
                            </group>
                        </page>
                        <page string="الاعتماد" name="signoff">
                            <group>
                                <group string="معد النموذج">
                                    <field name="prepared_name"/>
                                    <field name="prepared_date"/>
                                    <field name="prepared_phone"/>
                                    <field name="prepared_signature" widget="signature"/>
                                </group>
                                <group string="مشرف الورشة">
                                    <field name="supervisor_name"/>
                                    <field name="supervisor_date"/>
                                    <field name="supervisor_section"/>
                                    <field name="supervisor_signature" widget="signature"/>
                                </group>
                                <group string="مدير العمليات">
                                    <field name="manager_name"/>
                                    <field name="manager_date"/>
                                    <field name="manager_signature" widget="signature"/>
                                    <field name="manager_stamp" widget="image"/>
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <record id="view_work_order_search" model="ir.ui.view">
        <field name="name">rwasi.work.order.search</field>
        <field name="model">rwasi.work.order</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="sale_order_id"/>
                <field name="partner_id"/>
                <field name="product_id"/>
                <filter name="filter_draft" string="مسودة" domain="[('state', '=', 'draft')]"/>
                <filter name="filter_in_production" string="قيد التصنيع" domain="[('state', '=', 'in_production')]"/>
                <filter name="filter_delivered" string="تم التسليم" domain="[('state', '=', 'delivered')]"/>
                <group>
                    <filter name="group_state" string="الحالة" context="{'group_by': 'state'}"/>
                    <filter name="group_scope" string="القسم" context="{'group_by': 'workshop_scope'}"/>
                    <filter name="group_partner" string="العميل" context="{'group_by': 'partner_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="action_work_order" model="ir.actions.act_window">
        <field name="name">أوامر التصنيع</field>
        <field name="res_model">rwasi.work.order</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="view_work_order_search"/>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">لا توجد أوامر تصنيع بعد</p>
            <p>تُنشأ أوامر التصنيع تلقائياً عند تأكيد أمر بيع يحتوي منتجات يتم تصنيعها في الورشة.</p>
        </field>
    </record>

    <!-- ===== تحليلات أوامر التصنيع (Pivot / Graph) ===== -->
    <record id="view_work_order_pivot" model="ir.ui.view">
        <field name="name">rwasi.work.order.pivot</field>
        <field name="model">rwasi.work.order</field>
        <field name="arch" type="xml">
            <pivot string="تحليل أوامر التصنيع">
                <field name="workshop_scope" type="row"/>
                <field name="state" type="col"/>
                <field name="product_qty" type="measure"/>
            </pivot>
        </field>
    </record>

    <record id="view_work_order_graph" model="ir.ui.view">
        <field name="name">rwasi.work.order.graph</field>
        <field name="model">rwasi.work.order</field>
        <field name="arch" type="xml">
            <graph string="تحليل أوامر التصنيع" type="bar">
                <field name="workshop_scope"/>
                <field name="product_qty" type="measure"/>
            </graph>
        </field>
    </record>

    <record id="action_work_order_analysis" model="ir.actions.act_window">
        <field name="name">تحليل أوامر التصنيع</field>
        <field name="res_model">rwasi.work.order</field>
        <field name="view_mode">graph,pivot,list</field>
        <field name="search_view_id" ref="view_work_order_search"/>
    </record>

</odoo>
