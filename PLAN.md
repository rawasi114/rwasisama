# خطة تنفيذ موديول الإنشاءات والمقاولات — رواسي سما
## PLAN.md — وثيقة الخطة والفهم (للاعتماد قبل البدء)

> **هذه الوثيقة هي استجابة للقسم ٩ من `SPEC.md` (الخطوات الفورية).**
> **لن أكتب أي سطر برمجي لموديولات النظام حتى تعتمد هذه الخطة.**
> **اللغة:** عربية مع مصطلحات تقنية إنجليزية، حسب التعليمة الإلزامية رقم ٧.

---

## ١. ملخص تنفيذي (Executive Summary) — فهمي للمشروع

المطلوب بناء **موديول ERP متكامل لإدارة الإنشاءات والمقاولات** لشركة رواسي سما،
يحلّ تشتّت الأدوات الحالي (Excel + Odoo + Zoho + PlanRadar) عبر نظام واحد
يؤتمت **الدورة المستندية الكاملة** من الفرصة (Opportunity) إلى الإغلاق (Closeout).

**الفهم الجوهري الذي بنيت عليه كل قراراتي:**

1. **بند جدول الكميات هو النواة الذرية للنظام (BOQ Item is the Atomic Unit).**
   كل حركة — طلب مواد، أمر شراء، استلام، اعتماد مادة، إنجاز، مستخلص، أمر تغيير —
   ترتبط ببند BOQ عبر `boq_item_id`. هذه ليست توصية بل **قيد معماري صارم** سأفرضه
   على مستوى قاعدة البيانات (Foreign Keys + NOT NULL حيث يلزم) وعلى مستوى منطق الأعمال.

2. **الذكاء التسعيري هو الميزة الاستراتيجية المميِّزة.**
   الهدف النهائي ليس مجرد تسجيل البيانات، بل بناء **ذاكرة مؤسسية** تجعل كل منافسة
   أكثر تنافسية من سابقتها. لذلك حفظ الأسعار (الرابحة والخاسرة) في
   `price_intelligence_master` يجب أن يحدث **تلقائياً** عند كل تسعير، بدون تدخل المستخدم.

3. **الاستيراد الذكي لقوالب اعتماد متطلب حرج وغير تافه.**
   القوالب متنوّعة (Variant A بعشرة أعمدة، Variant B بتسعة أعمدة، وترتيب أعمدة مختلف).
   الحل الوحيد الصحيح هو **المطابقة بقيم العناوين العربية بعد تطبيع نصي قوي
   (Header-Based, Not Position-Based)**، مع منطق Forward Fill للفئة والمجموعة،
   وخدمة تطبيع وحدات (Units Normalization) وخدمة رموز SBC.

4. **هذا نظام حكومي/عسكري سعودي.** الأمان (RBAC دقيق، Audit Logs، Input Validation
   على الـ Backend) والتوطين (RTL كامل، تقويم هجري، ضريبة ١٥٪، ZATCA، تنسيق ر.س)
   متطلبات من الدرجة الأولى وليست تحسينات لاحقة.

5. **التسليم على مراحل ببوابات اعتماد.** لن أبني الموديولات الستة دفعة واحدة.
   سأتبع المراحل صفر→ثمانية بالترتيب، وأتوقف بعد كل مرحلة لتقرير
   (`PHASE_X_REPORT.md`) وانتظار موافقتك.

---

## ٢. تأكيد المبدأ المعماري المحوري

سألتزم بـ **"BOQ Item is the Atomic Unit"** عملياً عبر:

- جعل `boq_item_id` حقلاً إلزامياً (NOT NULL FK) في: `material_requests`,
  `purchase_orders` (على مستوى السطر), `goods_receipt_notes`,
  `material_approval_submissions`.
- جعله اختيارياً (nullable FK) حيث يكون منطقياً: `non_conformance_reports`,
  `rfis`, `variation_orders` (عبر جدول وسيط للبنود المتأثرة).
- بناء **View/خدمة تجميعية** تحسب لكل بند: التكلفة المخططة، المنفقة، المرتبطة
  (pending approvals)، والمتبقي — وهي أساس "فحص الميزانية الآلي".
- **اختبار معماري (Architecture Test)** يفشل إذا أُضيف جدول حركة جديد بدون رابط
  واضح لبند BOQ.

---

## ٣. المكدّس التقني (Tech Stack) — تأكيد مع تعديلات مبرَّرة

المكدّس المقترح في القسم ٢ ممتاز ومناسب لنظام ERP. **أؤكده بالكامل** مع الملاحظات التالية:

### Backend — مؤكَّد
| المكوّن | الاختيار | ملاحظة |
|---|---|---|
| Runtime | Node.js 20 LTS | مؤكَّد |
| Framework | **NestJS** | مؤكَّد — المعمارية المودولرية و DI مثاليان لـ ERP |
| ORM | **Prisma 5+** | مؤكَّد — يحمي من SQL Injection افتراضياً |
| Validation | class-validator + class-transformer | مؤكَّد |
| Auth | Passport + JWT + Refresh Tokens | مؤكَّد (access ١٥د) |
| Authorization | **CASL** | مؤكَّد — أفضل تكاملاً مع NestJS من casbin |
| Jobs | BullMQ + Redis | مؤكَّد — للاستيراد ودمج PDF |
| Excel | **SheetJS (xlsx)** | مؤكَّد — أساس الاستيراد الذكي |
| PDF | **pdf-lib** | مؤكَّد — لدمج وتوليد أغلفة MAS |
| Logging | Pino + correlation IDs | مؤكَّد |

### قرار يحتاج اعتمادك: نمط الـ API
- **توصيتي: REST + OpenAPI/Swagger auto-docs.**
- السبب: التعليمة الإلزامية رقم ١٠ تطلب توثيق OpenAPI تلقائي، ووجود دور
  "الاستشاري الخارجي" قد يحتاج واجهة API موثّقة. tRPC ممتاز لكنه يربط الـ Frontend
  والـ Backend بإحكام ولا يولّد OpenAPI بسهولة.
- (إن فضّلت tRPC رغم ذلك، أخبرني.)

### Frontend — مؤكَّد مع نقطة قرار واحدة
| المكوّن | الاختيار | ملاحظة |
|---|---|---|
| Framework | Next.js 14+ (App Router) + TS | مؤكَّد |
| UI | TailwindCSS + shadcn/ui (RTL) | مؤكَّد |
| State | Zustand + TanStack Query | مؤكَّد |
| Forms | React Hook Form + Zod | مؤكَّد |
| Tables | TanStack Table v8 | مؤكَّد — مع Virtual Scrolling لجداول الـ ١٠٠٠ بند |
| Charts | Recharts | مؤكَّد |
| Dates | dayjs + plugin هجري | مؤكَّد |
| i18n | next-intl | مؤكَّد |

### قرار يحتاج اعتمادك: مكتبة Gantt
| الخيار | الميزة | العيب |
|---|---|---|
| **frappe-gantt** (مجاني) | بدون تكلفة، مفتوح المصدر | المسار الحرج (Critical Path) و Baseline vs Actual يحتاجان تطويراً يدوياً |
| **dhtmlx-gantt** (تجاري) | Critical Path و Baseline و علاقات FS/SS/FF/SF جاهزة | يحتاج **ترخيص مدفوع** |
- متطلبات القسم ٢ (الموديول الثاني) تشمل Critical Path و Baseline vs Actual وعلاقات
  متقدمة — وهي جاهزة في dhtmlx فقط.
- **توصيتي:** نبدأ بـ frappe-gantt في المرحلة الثانية لإثبات المفهوم، ونرفّع إلى
  dhtmlx لاحقاً إذا اعتمدت ميزانية الترخيص. أحتاج قرارك بشأن الترخيص.

### Database & DevOps — مؤكَّد
PostgreSQL 16 (مع `pg_trgm` للبحث العربي و GIN index للـ full-text)، Redis 7،
تخزين S3-compatible (MinIO للتطوير)، Docker Compose، GitHub Actions.

---

## ٤. هيكل المستودع (Monorepo)

سأتبع الهيكل المقترح في القسم ٧ بالضبط، باستخدام **pnpm workspaces + Turborepo**
لإدارة الـ monorepo (أخف وأسرع من Nx لهذا الحجم):

```
rawasi-erp/
├── apps/web/      (Next.js)
├── apps/api/      (NestJS)
├── packages/shared-types/   (DTOs + enums مشتركة)
├── packages/ui/             (مكونات RTL مشتركة)
├── packages/utils/          (تطبيع عربي، تواريخ هجرية، تنسيق عملة)
├── prisma/
├── docker-compose.yml
└── ...
```

**ملاحظة:** خدمات التطبيع (Arabic normalization, Units, SBC) ستوضع في
`packages/utils` أو `apps/api/src/common` لتكون قابلة لإعادة الاستخدام والاختبار
المعزول، لأنها قلب الاستيراد الذكي والذكاء التسعيري.

---

## ٥. منهجية العمل (Methodology)

- **Phased Delivery:** مرحلة واحدة في كل مرة، بوابة اعتماد بينها.
- **Commit-by-feature:** Conventional Commits (feat/fix/refactor/test/docs).
- **اختبارات قبل الإكمال:** Unit + Integration، تغطية ≥ ٧٠٪ لطبقة منطق الأعمال.
  الاستيراد الذكي والتطبيع والذكاء التسعيري ستُغطّى بكثافة (هي الأخطر).
- **توثيق إلزامي:** تعليق لكل دالة/endpoint/جدول + OpenAPI تلقائي.
- **الأمان أولاً:** كل endpoint عليه Auth + Authz + Input Validation على الـ Backend.
- **تقرير بوابة:** `PHASE_X_REPORT.md` عند نهاية كل مرحلة ثم توقّف وانتظار.

---

## ٦. تقدير زمني واقعي لكل مرحلة

> التقديرات بافتراض تطوير مركّز ومتدرّج. أعرضها كنطاقات (أدنى–أعلى) لأن بعض
> القرارات المعلّقة (Gantt، الخطوط، الـ fixtures) تؤثّر على المدة الفعلية.

| المرحلة | الوصف | تقدير الجهد |
|---|---|---|
| **صفر** | التأسيس: Monorepo, Docker, Prisma, Auth+RBAC, Files, Audit, Login + Dashboard فارغ + RTL | ٣–٥ جلسات عمل |
| **الأولى** | المنافسات + الاستيراد الذكي من Excel + التسعير + التكاليف غير المباشرة + التحويل لمشروع | ٦–٩ جلسات (الأثقل) |
| **الثانية** | WBS بالمراحل الخمس + ربط BOQ + Gantt + استيراد الجدول | ٥–٧ جلسات |
| **الثالثة** | MR + فحص الميزانية + منطق التجاوز/القفل + PO + GRN + لوحة الميزانية | ٥–٧ جلسات |
| **الرابعة** | DMS + MAS مع دمج PDF + DSR + NCR + RFI | ٥–٧ جلسات |
| **الخامسة** | الذكاء التسعيري: المطابقة الفجوية + Side Panel + التقارير | ٤–٦ جلسات |
| **السادسة** | أوامر التغيير + المستخلصات + الضمانات البنكية | ٥–٧ جلسات |
| **السابعة** | Executive/Project/Risk Dashboards + التصدير | ٣–٥ جلسات |
| **الثامنة** | ZATCA + اعتماد + WhatsApp + Mobile + اختبارات أداء/أمان | مرنة |

> "جلسة العمل" = وحدة تطوير متماسكة تنتهي بكود قابل للتشغيل والاختبار، وليست
> قياساً زمنياً صارماً. سأحدّث التقدير عند نهاية كل بوابة بناءً على الواقع.

---

## ٧. المخاطر التقنية (Technical Risks)

| المخاطرة | الأثر | التخفيف |
|---|---|---|
| **غياب ملفّي الـ fixtures** (`variant_a/b.xlsx`) | لا يمكن تطوير/اختبار الاستيراد الذكي بدقة | **أحتاجهما قبل المرحلة الأولى.** بدونهما سأبني عيّنات اصطناعية مطابقة للمواصفات، لكنها لن تضمن مطابقة الواقع ١٠٠٪ |
| البحث النصي العربي (full-text) | دقة المطابقة في الذكاء التسعيري | استخدام `pg_trgm` + تطبيع قوي؛ والتحقق من هدف الدقة ≥ ٨٥٪ على عيّنة |
| دمج PDF بخطوط عربية مضمّنة (DIN Next LT Arabic) | غلاف MAS يتطلب Font Embedding | **أحتاج ملفات الخط المرخّصة**؛ بديل مؤقت: خط عربي مفتوح (Cairo/Noto) حتى تتوفّر |
| Critical Path في Gantt | قيد على مكتبة Gantt | قرار الترخيص (البند ٣) |
| أداء جداول الـ ١٠٠٠+ بند | تجربة المستخدم في التسعير | Pagination + Virtual Scrolling + فهارس مناسبة |
| التقويم الهجري الدقيق | تطابق التواريخ الرسمية | استخدام مكتبة موثوقة (umalqura) والتحقق من دقتها |

---

## ٨. الأسئلة المفتوحة (Open Questions) — أحتاج إجاباتك قبل البدء

> هذه الأسئلة جوهرية. الافتراض الخاطئ في ERP يعني إعادة عمل لأسابيع (التعليمة ٨).

1. **ملفّا الـ fixtures الإلزاميان مفقودان من المستودع.** الملحق أ ينصّ على أنهما
   "مرفقان كـ Test Fixtures إلزاميان"، لكنهما غير موجودين. **هل يمكنك رفعهما؟**
   (أحتاجهما لتطوير الاستيراد الذكي بدقة ولتحقيق معايير القبول في الملحق أ.٩.)

2. **نمط الـ API:** هل توافق على **REST + OpenAPI** (توصيتي)، أم تفضّل tRPC؟

3. **مكتبة Gantt:** هل نبدأ بـ **frappe-gantt المجاني** (مع تطوير يدوي للمسار الحرج)،
   أم تعتمد ميزانية ترخيص **dhtmlx-gantt** التجاري؟

4. **ملفات خط DIN Next LT Arabic:** هل لديك ترخيص/ملفات الخط لتضمينها في PDF؟
   أم أستخدم خطاً عربياً مفتوحاً مؤقتاً؟

5. **بيئة النشر (Deployment):** أين سيُشغَّل النظام؟ (سيرفر داخلي للشركة /
   سحابة AWS أو Cloudflare R2 / آخر)؟ هذا يؤثّر على إعداد التخزين والـ CI/CD.

6. **المصادقة:** هل يوجد مزوّد هوية حالي (SSO / Active Directory) للتكامل معه،
   أم أبني نظام مصادقة مستقلاً من الصفر؟

7. **القائمة المرجعية للموردين المعتمدين محلياً** (`local_content_certified_suppliers`،
   مذكورة في الموديول الثالث): هل لديك مصدر بيانات لها، أم نبدأ بإدخال يدوي؟

8. **أولوية التكاملات الخارجية** (ZATCA، منصة اعتماد، WhatsApp): مؤجّلة للمرحلة
   الثامنة كما في الخطة — أؤكّد ذلك، فقط تنبيهاً.

9. **نطاق الـ fixtures وقاعدة SBC:** القسم ٤ يذكر أن القائمة الكاملة لرموز SBC
   "تُستكمل عبر بحث منفصل". **هل أعتمد القائمة المُلاحظة في الملحق أ.٦ فقط كـ Seed
   أولي**، ونوسّعها لاحقاً؟ أم لديك ملف رموز SBC كامل؟

---

## ٩. الخطوات التالية المقترحة (بانتظار اعتمادك)

1. **تعتمد هذه الخطة** أو تطلب تعديلات.
2. تجيب على الأسئلة المفتوحة (خصوصاً ١، ٢، ٣، ٤).
3. (مثالياً) ترفع ملفّي الـ fixtures.
4. عند الاعتماد، أبدأ **المرحلة صفر (التأسيس)** بالضبط، ولا أتجاوزها، ثم أسلّم
   `PHASE_0_REPORT.md` وأتوقّف لانتظار موافقتك.

---

**ملاحظة:** أنشأت هذه الخطة على فرع `claude/bold-gates-esd4h` وسأفتح بها Pull Request
بصيغة **Draft** ليكون وعاءً للمراجعة والاعتماد. لن أدمج شيئاً أو أبدأ المرحلة صفر
قبل موافقتك الصريحة.
