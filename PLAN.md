# PLAN.md — خطة تنفيذ موديول الإنشاءات والمقاولات (رواسي سما)

> **الحالة:** مسودّة للمراجعة والاعتماد (Draft — Awaiting CEO Approval)
> **المرجع:** `SPEC.md` — دستور المشروع التقني الشامل
> **المُعِدّ:** Claude Code (مطوّر Full-Stack)
> **التاريخ:** 2026-05-25
>
> هذا الملف هو المخرج الإلزامي الأول المطلوب في القسم ٩ من `SPEC.md`.
> **لن أكتب أي سطر كود (Phase Zero) قبل اعتمادكم لهذه الخطة** (التزاماً بالقاعدة ٠.١ و ٠.٣).

---

## 1. الفهم التنفيذي (Executive Summary)

قرأتُ دستور المشروع بالكامل (١١٧٢ سطراً) وحلّلتُ ملفّي الاختبار المرفقين فعلياً. فهمي للمشروع كالتالي:

**المطلوب:** بناء موديول ERP متكامل لإدارة دورة حياة مشاريع المقاولات الحكومية/العسكرية في شركة رواسي سما، من **الفرصة (Opportunity)** في منصة اعتماد حتى **الإغلاق (Closeout)** والمستخلص النهائي — مع **ذاكرة تسعير مؤسسية** تتراكم مع كل منافسة لزيادة تنافسية العروض.

**المبدأ المعماري الحاكم (سأبني كل شيء حوله):**
> **"بند جدول الكميات هو الكيان المحوري" — BOQ Item is the Atomic Unit.**
> كل حركة (طلب مواد، أمر شراء، استلام، اعتماد مادة، إنجاز، مستخلص، أمر تغيير، NCR) ترتبط بـ `boq_item_id` واحد على الأقل. هذا يضمن: تقارير تكلفة على مستوى البند، بناء آلي لقاعدة الأسعار المرجعية، تتبّع إنجاز دقيق، ومنع التجاوزات آلياً.

**القيمة الاستراتيجية:** ليس مجرد نظام إدخال بيانات، بل أداة تسعير قتالية (Knockout Pricing بهامش ٥٪–٣٠٪) تذكر كل سعر سابق (رابح/خاسر) وتوصي بنطاق الفوز.

**أصحاب المصلحة وأدوارهم:** ٧ أدوار (CEO، مدير المشاريع، مدير المكتب الفني، مهندس التخطيط، مهندس الموقع، المحاسب، الاستشاري الخارجي) بصلاحيات RBAC دقيقة (مهندس الموقع لا يرى التكاليف، المحاسب لا يعدّل WBS، الاستشاري عرض فقط).

---

## 2. تأكيد المكدّس التقني (Tech Stack Confirmation)

أؤكّد المكدّس المقترح في القسم ٢ بالكامل تقريباً، مع تعديلات طفيفة مبرّرة:

| الطبقة | المُعتمَد | ملاحظة / تبرير أي تعديل |
|---|---|---|
| **Monorepo** | pnpm workspaces + Turborepo | إدارة `apps/*` و `packages/*` بكفاءة وبناء متزايد (cache). |
| **Frontend** | Next.js 14+ (App Router) + TypeScript | كما هو في الدستور. |
| **UI** | TailwindCSS + shadcn/ui (RTL) | كما هو. سأضبط `dir="rtl"` و Tailwind logical properties. |
| **State** | Zustand + TanStack Query | كما هو. |
| **Forms** | React Hook Form + Zod | كما هو. Zod schemas مشتركة بين FE/BE في `packages/shared-types`. |
| **Tables** | TanStack Table v8 | مع Virtual Scrolling لشاشة التسعير (١٠٠٠+ بند). |
| **Gantt** | ⚠️ قرار مفتوح — `frappe-gantt` (مجاني) كافتراضي | dhtmlx-gantt تجاري برخصة مدفوعة؛ أقترح frappe-gantt مبدئياً والترقية لاحقاً عند الحاجة لـ Critical Path المتقدّم. **يحتاج قراركم (سؤال مفتوح #4).** |
| **Backend** | NestJS (Node.js 20 LTS) | مودولر + DI، مثالي لـ ERP. كما هو. |
| **API Style** | ✅ **REST + OpenAPI** (وليس tRPC) | رغم أن الواجهة واحدة، أرشّح REST لأن الدستور يتطلب تكاملات مستقبلية (اعتماد، ZATCA، تطبيق جوال، WhatsApp). REST أسهل للتكامل الخارجي. |
| **ORM** | Prisma 5+ | كما هو. |
| **Validation** | class-validator + class-transformer + Zod | كما هو. |
| **Auth** | Passport.js + JWT + Refresh Tokens | access 15د، refresh أطول، rotation. |
| **Authorization** | CASL | RBAC دقيق على مستوى Resource+Action. |
| **Jobs** | BullMQ + Redis | لاستيراد Excel ودمج PDF وتوليد التقارير. |
| **PDF** | pdf-lib (+ تضمين خطوط عربية) | لدمج اعتمادات المواد (MAS) وتوليد التقارير. |
| **Excel** | SheetJS (xlsx) | للقراءة والكتابة — هو ما يفترضه الكود المرجعي في الملحق أ.٨. |
| **DB** | PostgreSQL 16 + `pg_trgm` + GIN | للبحث النصي العربي والمطابقة الفجوية للأسعار. |
| **Cache** | Redis 7 | كما هو. |
| **Storage** | S3-compatible (MinIO للتطوير) | تخزين الملفات منفصل عن DB. |
| **DevOps** | Docker Compose + GitHub Actions | كما هو. |
| **Logging/Monitoring** | Pino (correlation IDs) + Sentry | كما هو. |

**خلاصة:** لا انحراف جوهري عن الدستور. التعديلان الوحيدان: (أ) ترشيح REST بدل tRPC للتكامل المستقبلي، (ب) ترشيح frappe-gantt مبدئياً (قابل للنقاش).

---

## 3. القرارات المعمارية الأساسية (Key Architectural Decisions)

1. **BOQ Item محوري:** `boq_items` هو الجدول النواة. أي ميزة جديدة تُسأل: "كيف ترتبط ببند BOQ؟".
2. **الفئة والمجموعة كحقول نصية مطبّعة، لا كجداول حاوية:** تحليل Variant B أثبت أن "الفئة" و"البند" وسوم تصنيفية متكررة ومتفرّقة وليست هرمية صارمة (انظر §4 أدناه). لذلك **لن أُنشئ علاقة Parent-Child في `boq_sections`** بل أخزّن `category_name_ar`/`group_name_ar` نصيّاً مع نسخ مطبّعة مفهرسة، وأوفّر Views إحصائية للتجميع.
3. **الاستيراد بمطابقة العناوين لا بالمواقع (Header-Based):** القلب التقني للموديول الأول. خوارزمية واحدة تنجح على كل المتغيّرات.
4. **تطبيع الوحدات خدمة مركزية:** `UnitsNormalizationService` بقاموس aliases وخوارزمية تطبيع من ٧ خطوات (الملحق أ.٥). كل كتابات الوحدة الواحدة → نفس `unit_id`.
5. **معاملات ذرّية (Transactions):** الاستيراد والترسية و GRN — كلها إما تنجح كاملة أو تُلغى (لا نجاح جزئي).
6. **عمليات ثقيلة في الخلفية:** استيراد Excel ودمج PDF عبر BullMQ مع تقدّم Real-time (WebSocket).
7. **Audit-first:** كل عملية حساسة (اعتماد، تجاوز ميزانية، تعديل BOQ) تُسجَّل في `audit_logs` مع IP و user-agent و before/after state.
8. **الأمان على الـ Backend دائماً:** كل endpoint محمي (Auth + RBAC) وكل إدخال يُتحقَّق منه على الخادم بغض النظر عن الواجهة.

---

## 4. تصميم الاستيراد الذكي — مُتحقَّق من ملفّي الاختبار فعلياً (Verified Against Fixtures)

حلّلتُ الملفين برمجياً، وأؤكّد مطابقتهما للملحق أ مع الأرقام الدقيقة التالية (سأبني الاختبارات عليها):

| الخاصية | Variant A (مكاتب أمنية) | Variant B (قاعات حفر الباطن) |
|---|---|---|
| عدد الأعمدة | **١٠** | **٩** |
| صفوف البيانات الفعلية | **٤١** (الدستور قدّرها ~٣٩) | **٢٠٩** ✅ |
| موقع "الكمية" | العمود E (index 4) | العمود I (index 8، الأخير) |
| موقع "وصف البند" | العمود F (index 5) | العمود E (index 4) |
| عمود "مرفقات" | موجود | **غير موجود** ❌ |
| نتيجة `has_attachments` | **FALSE** (كلها "لا يوجد مرفقات") | **NULL** (لا عمود) |
| عمود "الفئة" | **فارغ تماماً** → كله NULL | مملوء متفرّقاً → **٢٣ فئة** بعد Forward Fill |
| `mandatory_local` | كله "نعم" → TRUE | ١٦٧ نعم (٨٠٪) / ٤٢ لا |
| `construction_code` | كله "غير محدد" → NULL | NULL×٤٢ + **٥١ رمز SBC رقمي مميّز** (2002, 2003, 2009, 2011, 2026, 2031, 2035–2058, 2063–2067, 2073–2106, 2124–2172, 2183…) |
| أطول وصف | ٧٩١ حرف | **١٦٩٦ حرف** (يؤكّد حالة الوصف الطويل) |

**كتابات الوحدات الفعلية المُلاحظة (حرجة للتطبيع):**

| الوحدة الموحّدة | الكتابات في Variant A | الكتابات في Variant B | يجب أن تنهار إلى |
|---|---|---|---|
| SQM (م²) | "متر مربع" | "م²", "م2" | `unit_id = SQM` |
| CBM (م³) | — | "م3" | `unit_id = CBM` |
| LM (م.ط) | "م ط", "م/ط" | "م.ط", "م ط", "م . ط" | `unit_id = LM` |
| EA (عدد) | "عدد" | "عدد" | `unit_id = EA` |
| SET (مجموعة) | "مجموعة" | — | `unit_id = SET` |
| SYS (نظام) | — | "نظام" | `unit_id = SYS` |
| KG (كجم) | — | "كجم" | `unit_id = KG` |

**خوارزمية الاستيراد (مطابقة للكود المرجعي في الملحق أ.٨):**
1. **كشف القالب:** قراءة صف العناوين، تطبيع عربي، مطابقة مع `ETIMAD_HEADERS_DICTIONARY` لبناء `field → column_index` ديناميكياً.
2. **التحقق الإلزامي:** وجود `serial_index, description, unit, quantity` وإلا → الوضع المرن (Flexible Import / Column Mapping يدوي).
3. **القراءة مع Forward Fill** على `category` و `group` (الخلية الفارغة ترث آخر قيمة ممتلئة أعلاها).
4. **التطبيع:** الوحدة عبر `UnitsNormalizationService`؛ `construction_code` ("غير محدد"/فارغ → NULL، رقم → TEXT + جلب اسم SBC من Lookup)؛ `mandatory_local` (نعم/لا/NULL)؛ `attachments` (غياب العمود → NULL).
5. **تقرير تجميعي** بعد الاستيراد (عدد البنود، الفئات، رموز SBC المعروفة/المجهولة، البنود الإلزامية، تحذيرات الوحدات الجديدة).

> **ملاحظة دقّة:** بعض رموز SBC في Variant B (مثل 2009, 2127, 2153, 2158, 2159, 2165, 2172) موجودة فعلاً في الملف لكنها خارج قائمة الـ Seed المختصرة في القسم ٤. النظام سيتعامل معها بأمان: الرمز يُحفظ كـ TEXT دائماً، واسم القسم يبقى NULL إن لم يكن في `sbc_codes_lookup` (مع وسمه في تقرير الاستيراد كـ "رمز غير معروف"). هذا مقصود ومذكور في الدستور ("47 منها معروف"). **سؤال مفتوح #14** حول مصدر القائمة الكاملة.

---

## 5. استراتيجية قاعدة البيانات (Database Strategy)

- سأبني `prisma/schema.prisma` على المخطط الأوّلي في القسم ٤، مع توثيق كل إضافة.
- **Seed Data إلزامية من البداية:** `units_lookup` (١٣ وحدة + aliases)، `sbc_codes_lookup` (الرموز المُلاحظة)، `roles` & `permissions` (الأدوار السبعة)، مستخدم admin أوّلي.
- **الفهارس:** composite على `(opportunity_id, serial_index)` و `(project_id, serial_index)`؛ فهارس على `category_normalized`, `group_normalized`, `construction_code`, `is_mandatory_local_product`؛ **GIN + pg_trgm** على `description_normalized` لمحرّك المطابقة.
- **Migrations منظّمة** قابلة للترقية بدون فقدان بيانات.
- **`boq_items_history`** لحفظ النسخ قبل أوامر التغيير (VO).

---

## 6. استراتيجية الاختبار (Testing Strategy)

- **الهدف:** ≥ ٧٠٪ تغطية لطبقة منطق الأعمال (Business Logic).
- **Unit Tests:** خدمات التطبيع، الكشف، Forward Fill، فحص الميزانية، حسابات المستخلص (VAT ١٥٪، Retention، الدفعة المقدمة).
- **Integration Tests:** رفع الملفين الحقيقيين كـ Fixtures إلزاميين والتحقق من معايير القبول (الملحق أ.٩).
- **الـ ١٠ حالات الحافة (الملحق أ.٧)** ستكون اختبارات صريحة، أبرزها:
  - عمود مرفقات غير موجود (B) → `has_attachments = NULL`.
  - عمود فئة فارغ (A) → `category = NULL` لكل البنود.
  - Forward Fill عبر عشرات الصفوف وتكرار الفئة متفرّقة (B).
  - تطبيع "م . ط" (بفراغين) → `LM`؛ و "م²"/"م2" → `SQM`.
  - وصف ١٦٩٦ حرفاً يُحفظ كاملاً.
  - رفع ملف ليس BOQ → كشف يفشل → وضع مرن/رفض واضح.
- **E2E (لاحقاً):** Playwright للمسارات الحرجة (تسجيل فرصة → استيراد → تسعير → ترسية → مشروع).

---

## 7. الأمان والمتطلبات غير الوظيفية (Security & NFR)

- bcrypt (cost ≥ ١٢)، JWT (access ١٥د) + refresh rotation، Rate limiting، CSRF على التعديلات، XSS sanitization، Prisma (SQLi-safe).
- RBAC دقيق عبر CASL. كل عملية حساسة في `audit_logs`.
- الأداء: استيراد ٥٠٠ بند < ٣٠ث، شاشة تسعير ١٠٠٠ بند بلا تأخير، APIs < ٥٠٠ms قراءة.
- التوطين: RTL كامل، تقويم هجري/ميلادي، أرقام عربية/هندية، SAR ("1,234.50 ر.س")، VAT ١٥٪.
- سعودية: ZATCA E-Invoice Phase 2 (XML) للمستخلصات، هوية رواسي سما البصرية (Navy `#253747`, Gold `#BD9B5E`, خط DIN Next LT Arabic).

---

## 8. خطة المراحل والتقديرات الزمنية (Phased Delivery & Estimates)

> **منهجية:** بوابة (Checkpoint) بعد كل مرحلة + تقرير `PHASE_X_REPORT.md` + انتظار موافقتكم قبل المرحلة التالية (القاعدة ٠.٣).
> **ملاحظة على التقدير:** تقديرات الدستور بالأسابيع تفترض فريقاً. أُدرج أدناه تقديري كـ **جلسات عمل تكرارية (iterations)** مع مكافئ تقريبي بالأسابيع للرجوع. الأرقام قابلة للضبط بعد Phase 0.

| المرحلة | المحتوى | تقديري | مكافئ الدستور |
|---|---|---|---|
| **٠ — التأسيس** | Monorepo، Docker، CI/CD، Prisma + أول Migration، Auth + RBAC، Files، Audit، Login + Dashboard فارغة + RTL | ٢–٣ جلسات | أسبوع |
| **١ — الفرص والمشاريع** | `opportunities` CRUD، **الاستيراد الذكي لقالب اعتماد** (القلب)، التسعير (بلا ذكاء)، التكاليف غير المباشرة، معالج التحويل لمشروع، `projects` CRUD | ٤–٦ جلسات | أسبوعان |
| **٢ — WBS والجدول الزمني** | المراحل الخمس الإجبارية، WBS هرمي (٤ مستويات)، ربط المهام بـ BOQ، Gantt تفاعلي، رفع الجدول من Excel | ٣–٥ جلسات | أسبوعان |
| **٣ — الميزانية والمواد** | MR + فحص الميزانية الآلي، منطق التجاوز/القفل/الاعتماد العالي، PO، GRN، لوحة مراقبة الميزانية | ٣–٥ جلسات | أسبوعان |
| **٤ — المستندات والاعتمادات** | DMS + versioning + بحث عربي، **MAS + دمج PDF آلي**، DSR، NCR، RFI | ٣–٥ جلسات | أسبوعان |
| **٥ — الذكاء التسعيري** | `price_intelligence_master`، محرّك المطابقة الفجوية (pg_trgm)، Side Panel، التقارير | ٢–٣ جلسات | أسبوع |
| **٦ — المستخلصات وVO والضمانات** | أوامر التغيير + `boq_items_history`، توليد المستخلصات (VAT/Retention)، الضمانات البنكية + التنبيهات | ٣–٤ جلسات | أسبوعان |
| **٧ — لوحات التحكم** | Executive/Project/Risk Dashboards، تقارير PDF+Excel | ٢–٣ جلسات | أسبوع |
| **٨ — التحسينات والتكاملات** | ZATCA، اعتماد API، WhatsApp، Mobile responsive، اختبارات أداء/أمان | مرنة | مرنة |

---

## 9. منهجية Git والتسليم (Git & Delivery Workflow)

- فرع التطوير: `claude/keen-gates-KKqLj`.
- **Conventional Commits:** `feat`, `fix`, `refactor`, `test`, `docs`، commit واحد لكل ميزة منطقية.
- بعد كل مرحلة: `PHASE_X_REPORT.md` + توقّف بانتظار الموافقة.
- Pull Request (Draft) لمراجعتكم؛ توثيق OpenAPI/Swagger يُولَّد آلياً.

---

## 10. الأسئلة المفتوحة (Open Questions — تحتاج إجاباتكم) ⚠️

هذه نقاط غموض حقيقية أفضّل حسمها قبل/مع Phase 0 تفادياً لإعادة العمل (القاعدة ٠.٨ "لا تخترع"):

1. **الاستضافة وسيادة البيانات:** بما أن العملاء جهات حكومية/عسكرية — هل الاستضافة On-Premise أم سحابة داخل السعودية؟ هل هناك متطلبات NCA/SDAIA للامتثال؟
2. **المصادقة:** حسابات داخلية فقط مبدئياً، أم تكامل **نفاذ (Nafath)** / SSO مطلوب من الآن؟
3. **API Style:** أوافقون على REST + OpenAPI (ترشيحي) بدل tRPC؟
4. **مكتبة Gantt:** frappe-gantt (مجاني، ترشيحي) أم dhtmlx-gantt (تجاري، رخصة مدفوعة — هل الميزانية متاحة)؟
5. **التقويم الهجري:** أي مصدر/مكتبة معتمدة؟ (أقترح أم القرى عبر مكتبة موثوقة).
6. **خطوط الهوية للـ PDF:** دمج MAS يتطلب **تضمين** خط DIN Next LT Arabic — هل ملفات الخطوط (DIN Next LT Arabic + WinSoft Pro Medium) مرخّصة ومتاحة لي لتضمينها؟ (بدونها سأستخدم خطاً عربياً مفتوحاً بديلاً مؤقتاً مثل Cairo/IBM Plex Arabic).
7. **شعار وقالب MAS:** هل يتوفّر شعار الشركة (SVG/PNG عالي الدقة) وقالب نموذج الاعتماد الرسمي؟
8. **قائمة الموردين المعتمدين محلياً:** مصدر بيانات `local_content_certified_suppliers` — إدخال يدوي أم استيراد من هيئة المحتوى المحلي؟
9. **القائمة الكاملة لرموز SBC:** أكتفي بالرموز المُلاحظة في الملفين كـ Seed وأوسّع لاحقاً، أم تزوّدونني بالقائمة الرسمية الكاملة من هيئة المواصفات؟
10. **قابلية تهيئة مسار الاعتماد (Approval Workflow):** ثابت كما في الدستور (مهندس → مكتب فني → مدير عام)، أم قابل للتهيئة عبر الواجهة لكل مشروع؟
11. **تعدد الشركات (Multi-tenancy):** شركة واحدة فقط (رواسي سما)، أم تصميم يدعم كيانات متعددة لاحقاً؟
12. **اعتماد/WhatsApp/ZATCA:** أؤكّد أنها جميعاً مرحلة ٨ (تكاملات لاحقة) ولا حاجة لها في المراحل ٠–٧؟
13. **اللغة في التقارير المُولَّدة (PDF):** عربي فقط أم ثنائي اللغة (عربي/إنجليزي)؟

---

## 11. ما سأفعله فور الموافقة (Phase 0 Scope)

عند اعتمادكم لهذه الخطة، أبدأ **المرحلة صفر فقط**:
1. تهيئة Monorepo (pnpm + Turborepo) بالهيكل المقترح في القسم ٧ من الدستور.
2. `docker-compose.yml` (PostgreSQL 16، Redis 7، MinIO) + `.env.example`.
3. `prisma/schema.prisma` أوّلي + أول Migration + `seed.ts` (units, SBC, roles, admin).
4. NestJS API: `auth` (JWT + refresh) + `users` + RBAC (CASL) + `files` + `audit_logs` + Pino logging.
5. Next.js Web: إعداد RTL + i18n (next-intl) + صفحة Login + Dashboard فارغة بهوية رواسي سما.
6. CI (GitHub Actions): lint + test + build.
7. `PHASE_0_REPORT.md` ثم التوقّف بانتظار موافقتكم.

> **بانتظار:** (أ) اعتماد هذه الخطة، (ب) إجابات الأسئلة المفتوحة في القسم ١٠ (أو الموافقة على ترشيحاتي الافتراضية حيثما اقترحت).

— Claude Code
