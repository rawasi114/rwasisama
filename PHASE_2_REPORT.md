# تقرير المرحلة الثانية — هيكل تجزئة العمل (WBS) والجدول الزمني

> **الحالة:** ✅ النواة مكتملة ومُتحقَّق منها على **Odoo 19** (٢٣ اختبار يمر).
> **عرض Gantt:** في موديول جسر منفصل (Enterprise) — مُجهّز، يُثبَّت عندك.
> **بانتظار موافقتك قبل المرحلة الثالثة.**

---

## ١. ما تم بناؤه

### الموديلات
| الموديل | الوصف |
|---|---|
| `rawasi.wbs.phase` | قوالب **المراحل الخمس** القياسية (قابلة للتعديل) |
| `rawasi.wbs.activity` | نشاط WBS هرمي: تواريخ مخطّطة + **خط أساس (Baseline)** + فعلي + نسبة إنجاز + معالم + علاقات تسلسل (Predecessors) + **ربط ببنود BOQ** + تكلفة مخطّطة |
| `project.project` (توسعة) | أزرار «توليد المراحل الخمس» و«استيراد الجدول الزمني» + زر إحصائي لعدد الأنشطة |

### المراحل الخمس القياسية (Seed)
التعبئة والأعمال التمهيدية · الأعمال الإنشائية · أعمال العمارة والتشطيبات ·
الأعمال الكهروميكانيكية · الاختبار والتشغيل والتسليم.
تُولَّد تلقائياً عند تحويل المنافسة إلى مشروع، أو يدوياً بزر.

### ربط BOQ بالجدول (مبدأ النواة الذرية)
كل نشاط يرتبط ببنود `rawasi.boq.item` عبر علاقة m2m، وتُحسب **التكلفة المخططة**
للنشاط من مجموع تكاليف بنوده — فيبقى بند BOQ هو النواة التي يُبنى عليها الجدول.

### استيراد الجدول الزمني (`rawasi.schedule.import.wizard`)
استيراد ذكي بالعناوين (مثل استيراد BOQ): الأعمدة (النشاط، المرحلة، تاريخ البداية،
تاريخ النهاية، نسبة الإنجاز، معلَم)، مع **مطابقة المرحلة بالاسم**، تحليل التواريخ
بصيغ متعددة، ووسم المعالم، **ورفض الملفات غير المطابقة**.

### عرض Gantt (موديول جسر `rawasi_construction_gantt`)
- يعتمد على `web_gantt` (متوفّر في **Odoo Enterprise**).
- عرض Gantt لأنشطة WBS مع: التجميع حسب المرحلة، شريط الإنجاز، **أسهم العلاقات
  (dependencies)** عبر `predecessor_ids`.
- معزول عن النواة كي تبقى النواة متوافقة مع Community وقابلة للاختبار.

---

## ٢. التحقق الفعلي (Odoo 19)

```
0 failed, 0 error(s) of 23 tests   ✅
```
(١٧ من المرحلتين السابقتين + ٦ جديدة لـ WBS)

**تغطية اختبارات WBS الجديدة:**
- توليد المراحل الخمس (وعدم تكرارها).
- حساب المدة من التواريخ.
- منع تاريخ نهاية قبل البداية (ValidationError).
- حساب التكلفة المخططة من بنود BOQ المرتبطة.
- **استيراد الجدول الزمني** (٦ أنشطة، مطابقة المرحلة بالاسم، وسم المعلَم).
- رفض ملف غير صالح في استيراد الجدول.

> **حدود التحقق:** عرض Gantt لم يُختبر محلياً لأن `web_gantt` غير موجود في
> Community؛ لكنه مكتوب وفق صيغة Odoo 19 ومعزول في موديول مستقل تُثبّته على Enterprise.

---

## ٣. كيفية الاستخدام عندك (Enterprise)

1. **ترقية** موديول «رواسي سما» (يضيف WBS والجداول).
2. **تثبيت** موديول «رواسي سما — مخطط جانت (Gantt)» لإظهار عرض Gantt.
3. التدفّق: منافسة فائزة ← «تحويل لمشروع» (يولّد المراحل الخمس) ← من المشروع
   «استيراد الجدول الزمني» أو إضافة الأنشطة يدوياً وربطها ببنود BOQ ← عرض Gantt.

---

## ٤. الخطوة التالية

بانتظار موافقتك للمرحلة الثالثة: طلبات المواد (MR) + **فحص الميزانية الآلي** ضد
بنود BOQ + منطق التجاوز/القفل + أوامر الشراء (PO) + سندات الاستلام (GRN) + لوحة الميزانية.
